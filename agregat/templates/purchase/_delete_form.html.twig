<form method="post" action="{{ path('app_purchase_delete', {'id': purchase.id}) }}" onsubmit="return confirm('Вы уверены что хотите удалить этот элемент');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ purchase.id) }}">
    <button class="btn btn-danger">Удалить</button>
</form>
