{% extends 'base.html.twig' %}

{% block title %}Как купить{% endblock %}

{% block body %}
    <h1>Как купить</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Идентификатор</th>
                <td>{{ text.id }}</td>
            </tr>
            <tr>
                <th>Название</th>
                <td>{{ text.title }}</td>
            </tr>
            <tr>
                <th>Описание</th>
                <td>{{ text.description }}</td>
            </tr>
            <tr>
                <th>Подтип</th>
                <td>{{ text.getSubTypeName() }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_texts_index_how_buy') }}">Вернуться к списку</a>

    <a href="{{ path('app_texts_edit_how_buy', {'id': text.id}) }}">Редактировать</a>

    {{ include('howBuy/_delete_form.html.twig') }}
{% endblock %}
