<form method="post" action="{{ path('app_texts_delete_how_buy', {'id': text.id}) }}" onsubmit="return confirm('Вы уверены, что хотите удалить этот элемент?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ text.id) }}">
    <button class="btn btn-danger">Удалить</button>
</form>
