{{ form_start(form) }}
    {{ form_row(form.isPopular) }}
    {% if category.img %}
        <div>
            <img id="delete_category_img" src="{{ category.img }} " alt="����������� ���������" style="max-width: 250px;">
        </div>
    {% endif %}
    {{ form_row(form.img) }}
    {{ form_row(form.title) }}
    {{ form_row(form.description) }}
    <button class="btn btn-success">{{ button_label|default('Сохранить') }}</button>
<div hidden>
    {{ form_rest(form) }}
</div>
{{ form_end(form) }}

