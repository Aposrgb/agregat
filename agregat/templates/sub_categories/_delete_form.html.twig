<form method="post" action="{{ path('app_sub_categories_delete', {'id': sub_category.id}) }}" onsubmit="return confirm('Вы уверены, что хотите удалить этот элемент?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sub_category.id) }}">
    <button class="btn btn-danger">Удалить</button>
</form>
